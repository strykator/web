/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'popmenucloud.com',
      'img.cdn4dd.com',
      'firebasestorage.googleapis.com',
    ],
  },
}

module.exports = nextConfig
